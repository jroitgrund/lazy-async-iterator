{
    "name": "lazy-async-iterator",
    "scripts": {
        "prettier:fix":
            "find src -name '*.ts' -exec node_modules/.bin/prettier --tab-width 4 --print-width 100 --trailing-comma es5 --write {} +",
        "prettier":
            "find src -name '*.ts' -exec node_modules/.bin/prettier --tab-width 4 --print-width 100 --trailing-comma es5 --list-different {} +",
        "tslint:fix": "tslint --fix 'src/**/*.ts'",
        "tslint": "tslint 'src/**/*.ts'",
        "tsc": "tsc",
        "jest": "jest",
        "test": "npm-run-all tsc -p tslint prettier jest",
        "prepublish": "test"
    },
    "version": "0.0.1",
    "main": "lib/index.js",
    "license": "MIT",
    "devDependencies": {
        "@types/jest": "^21.1.2",
        "jest": "^21.2.1",
        "npm-run-all": "^4.1.1",
        "prettier": "^1.7.4",
        "tslint": "^5.7.0",
        "typescript": "^2.5.3"
    }
}
